nav.navbar.navbar-default.navbar-fixed-top
    
    
    div.container
        div.navbar-header
            a.navbar-brand(href="/")
                img(src="/images/stryker-80x80.png", alt="Stryker", width=20, height=20)
        div#navbar.collapse.navbar-collapse
            ul.nav.navbar-nav
                each val, key in menu
                    li(class=currentUrl === val ? 'active' : '')
                        a(href=val, title=key)= key

